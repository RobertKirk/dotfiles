sudo apt-get update
sudo apt-get install vim zsh spotifycli gnome-tweaks tmux curl wget wmctrl git ssh openssh

# vscode, spotify, firefox, wavebox, oh-my-zsh, fonts, anaconda, python latest, z.lua, lua5.3

cd ~
mkdir repos
touch .vimrc
touch .zshrc

# vscode plugins: vim, git, python, base16themes, gitlens, rest client, bracket pair colourizer, vscode-spotify

